# Session Log

> **Contexto de proyecto (siempre cargado):** `PROJECT_STATE.md` — fase, decisiones, pendientes.
> **Historial archivado:** `CHANGELOG/` — una semana por archivo (ver índice en PROJECT_STATE, §4).

---

## (sin entradas todavía)

> Cuando registres progreso (via `/record` o `summarizer`), añade entradas al inicio, tras este bloque:

```markdown
## YYYY-MM-DD — <Título corto>

**Qué:** <2-4 líneas: qué se hizo y por qué, decisiones clave>

**Verificación:** <comandos usados y resultado>
```

> No agregues listas de archivos. La rotación semanal (lunes o >150 líneas) es automática vía hook post-commit, o manual con `/rotate-memory`.

---

## Índice de historial archivado (CAPA 2)

| Semana (lunes) | Archivo |
|----------------|---------|
| (aún sin historial) | — |
